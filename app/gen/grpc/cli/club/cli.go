// Code generated by goa v3.13.2, DO NOT EDIT.
//
// club gRPC client CLI support package
//
// Command:
// $ goa gen app/design

package cli

import (
	bandc "app/gen/grpc/band/client"
	orderc "app/gen/grpc/order/client"
	"flag"
	"fmt"
	"os"

	goa "goa.design/goa/v3/pkg"
	grpc "google.golang.org/grpc"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() string {
	return `order tea
band play
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` order tea --message '{
      "includeMilk": true,
      "isGreen": true,
      "numberSugars": 4258777314432124607
   }'` + "\n" +
		os.Args[0] + ` band play --message '{
      "style": "Bebop"
   }'` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(cc *grpc.ClientConn, opts ...grpc.CallOption) (goa.Endpoint, any, error) {
	var (
		orderFlags = flag.NewFlagSet("order", flag.ContinueOnError)

		orderTeaFlags       = flag.NewFlagSet("tea", flag.ExitOnError)
		orderTeaMessageFlag = orderTeaFlags.String("message", "", "")

		bandFlags = flag.NewFlagSet("band", flag.ContinueOnError)

		bandPlayFlags       = flag.NewFlagSet("play", flag.ExitOnError)
		bandPlayMessageFlag = bandPlayFlags.String("message", "", "")
	)
	orderFlags.Usage = orderUsage
	orderTeaFlags.Usage = orderTeaUsage

	bandFlags.Usage = bandUsage
	bandPlayFlags.Usage = bandPlayUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "order":
			svcf = orderFlags
		case "band":
			svcf = bandFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "order":
			switch epn {
			case "tea":
				epf = orderTeaFlags

			}

		case "band":
			switch epn {
			case "play":
				epf = bandPlayFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "order":
			c := orderc.NewClient(cc, opts...)
			switch epn {
			case "tea":
				endpoint = c.Tea()
				data, err = orderc.BuildTeaPayload(*orderTeaMessageFlag)
			}
		case "band":
			c := bandc.NewClient(cc, opts...)
			switch epn {
			case "play":
				endpoint = c.Play()
				data, err = bandc.BuildPlayPayload(*bandPlayMessageFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// orderUsage displays the usage of the order command and its subcommands.
func orderUsage() {
	fmt.Fprintf(os.Stderr, `A waiter that brings drinks.
Usage:
    %[1]s [globalflags] order COMMAND [flags]

COMMAND:
    tea: Order a cup of tea.

Additional help:
    %[1]s order COMMAND --help
`, os.Args[0])
}
func orderTeaUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] order tea -message JSON

Order a cup of tea.
    -message JSON: 

Example:
    %[1]s order tea --message '{
      "includeMilk": true,
      "isGreen": true,
      "numberSugars": 4258777314432124607
   }'
`, os.Args[0])
}

// bandUsage displays the usage of the band command and its subcommands.
func bandUsage() {
	fmt.Fprintf(os.Stderr, `A band that plays jazz.
Usage:
    %[1]s [globalflags] band COMMAND [flags]

COMMAND:
    play: Choose your jazz style.

Additional help:
    %[1]s band COMMAND --help
`, os.Args[0])
}
func bandPlayUsage() {
	fmt.Fprintf(os.Stderr, `%[1]s [flags] band play -message JSON

Choose your jazz style.
    -message JSON: 

Example:
    %[1]s band play --message '{
      "style": "Bebop"
   }'
`, os.Args[0])
}
